# STATUS — trino-doris connector, 2026-07-19

State-of-the-connector summary at session end. Baseline pin: Apache Doris
`4.1.3-rc02-7126cf65d96` + Trino 483 + MySQL Connector/J 9.7.0 (any Doris version change
voids the evidence and requires re-probes — ledger header rule).

## Phases done

| Phase | Content | Commits |
|---|---|---|
| P0 | Protocol/type probe, ARRAY wire spike (GO-with-restrictions), stock-MySQL baseline (incl. the DELETE danger), StarRocks audit, plugin-assembly proof, brikk API audit, type & capability LEDGER | 51cd191, 8d03590, 923d6f7 |
| P1a | Module scaffold, information_schema-driven metadata, ledger §A scalar type contract, streaming, LIMIT, conservative domain pushdown (strings NULL-only), assembly verification task | a83cbb4 |
| P1b | Read-only defense in depth (ForwardingJdbcClient guard with reflection audit, connector access control, airtight system.execute denial, trino_ro SELECT-only account), cancellation + server-side query_timeout with measured numbers, query-id remote-SQL comments + audit-log test substrate, CONVERT_TO_VARCHAR resolution | a81629b |
| P2a | Production strict ARRAY wire decoder + native ARRAY(T) for the unambiguous element allowlist, live differential vs Doris oracle, string-array ambiguity kept non-native | 4725ee1 |
| P2b | Typed index-aware predicate rules: contains / arrays_overlap (guarded) / array_position comparisons, NULL truth-table pins for both engines, inverted-index EXPLAIN+profile evidence, DorisPushdownEvidence citation registry (brikk pin PENDING) | b56d12d |
| P3 slice | Safe TopN (native NULLS FIRST/LAST, non-text exact keys, guaranteed, >65535 proven), NOT/AND/OR composition over the value-safe tier only, dynamic catalog management proven (CREATE/DROP CATALOG parity, validation-at-create semantics) (uncommitted this session) | (this worktree) |
| P4 strings | Configurable string pushdown modes (`doris.string-pushdown.mode` / `string_pushdown_mode`: NULL_ONLY / GUARDED default / BINARY / FULL) grounded in a live byte-semantics probe; GUARDED = retained-filter superset pre-filter with 0x00 + CHAR exclusions; BINARY/FULL unlock VARCHAR TopN + LIKE (backslash-doubling rewrite, ESCAPE passthrough) | 03a67d1 |
| Timezone audit | TODO-timezones items 1–3 executed test-only (`TestDorisTimezoneAudit`): zone matrix over pushed temporal literals (DST-adversarial + 0000/9999 edges), session-zone-invariant datetime paths, doris.time-zone canary per pushed family — NO zone-dependence found; CI cancellation flake root-caused (killed query's processlist row lingers on stalled client socket) and release contract restructured to persistent fe.log evidence | (this worktree) |
| RO-MAX B3 | DISTINCT pushes (grouping-key gate widened to varchar — byte-exact grouping; count(DISTINCT text) collapses via engine decomposition); HAVING verified pushing as outer WHERE; domain-compaction knob documented (base default 256, session-tunable, big-IN evidence); dynamic filters verified reaching remote SQL; README coverage summary added — closes READ-ONLY-MAX (uncommitted this session) | (this worktree) |
| RO-MAX B2 | Aggregate expansion: min_by/max_by (NOT-NULL-value guard — NULL-payload divergence pinned), any_value, approx_distinct opt-in (doris.approximate-pushdown, default off); conditional aggregation ENGINE-DENIED at 483 (mask bail pinned to PushAggregationIntoTableScan:110); avg deny sharpened (offered whole, SPI 1:1, manual sum/count decomposition pushes exactly) (uncommitted this session) | (this worktree) |
| RO-MAX B1 | Scalar expression pushdown: coalesce/nullif, temporal extraction (year..second), lower/upper (identity battery-proven — divergence hunt EMPTY), length->char_length, starts_with->escaped LIKE-prefix; CASE/IF pinned ENGINE-DENIED at 483; Latin-1 guard sweep 255/255 exact; evidence pins 7->19 (uncommitted this session) | (this worktree) |
| Cancel fix | CLUSTER-SCOPED cancellation (user-reported multi-FE/LB defect: driver-level KILL targets an arbitrary FE): marker -> cluster-wide FULL processlist -> QueryId -> forwarded KILL, async helper, stock socket-abort belt retained; cross-FE proof on the overlay; trace-id kill pinned version-unstable (4.1.2 no-op, 4.1.3 works) and unused; credit: dev.sort.doris.cancel reference impl | (this worktree) |
| P6 | Evidence-tiered GUARDED (proven string shapes push fully — WHERE str = ... LIMIT n collapses; hazards stay local; LIKE-prefix retention now structural via the engine-retained LIKE); date-of-datetime projection pushdown + GROUP BY composition (CAST->DATE + date_trunc second..year, week/millisecond denied with evidence, predicate position free via engine unwrapping) (uncommitted this session) | (this worktree) |
| P5 | Remote statistics (row counts + column NDV/nulls/size/numeric ranges, fail-soft, `doris.statistics.enabled`); cost-aware JOIN pushdown OFF by default (INNER/LEFT/RIGHT, non-text exact keys, `<=>` for IS NOT DISTINCT FROM, AUTOMATIC strategy fed by the statistics reader); JSON/cardinality/LIKE-prefix pushdown batch (uncommitted this session) | (this worktree) |
| P4 aggregates | Verified aggregate pushdown: count(*)/count(col)/count(DISTINCT exact-types), min/max on the exact key set (NOT text — collation; NOT REAL/DOUBLE — Doris max=NaN vs Trino max=Infinity, live-pinned), sum(DECIMAL p≤18) only (Doris sums wrap SILENTLY at 2^64/2^128 where Trino throws — live-pinned), avg NEVER pushed (Doris truncates at scale max(s,4), corrupts at p=38; avg(bigint): Doris exact wide-int sum vs Trino lossy double accumulation), GROUP BY keys restricted to the exact set with NULL-grouping proofs; probe pins in `TestDorisP4AggregateProbes`, differentials/audit-log shapes in `TestDorisP4Aggregates`; full verdict table in `NOTES-p4-aggregates.md` | 2232037 |

**Tests: 168 across 20 suites** (incl. ServiceLoader-registration and brikk evidence-drift suites), all green in one `./gradlew :trino-doris:build`
(unit: decoder/type-mapping/config/read-only-reflection; live vs the compose cluster:
smoke, arrays, read-only denial, trino_ro, cancellation, remote-SQL audit, P2b pushdown,
inverted-index gates, P3 TopN, P3 composition, dynamic catalogs, P4 string modes). Plugin dir verified:
79 jars (brikk-sql-metadata + kotlinx-serialization-core/json joined the bundle), no
provided/parent-first SPI jars, ServiceLoader registration present.

## CLOSED / PARKED user decisions

1. **CLOSED (2026-07-19): brikk-house additive API + first release** —
   `dev.brikk.house:brikk-sql-metadata-jvm:0.7.0` is RELEASED on Maven Central and pinned
   (sha256 693575c17a041a0370b44a233969350f95d27c9bb47b14ebbbd1c80d2d3b1de8). brikk
   implemented the request direction-oriented (`FunctionHazard.sourceName`/`targetName`;
   `HazardRegistry.lookup` unchanged). The connector now: depends on it in production
   (PLAN G3 sanctions the featherweight module), cross-checks every pushdown rule's pinned
   evidence tuple against the registry AT CONNECTOR CONSTRUCTION (drift refuses to build the
   catalog, fail-loud), and runs `TestDorisPushdownEvidenceDrift` (tuple pins + G3
   verdict-gating structure + element_at/length canaries + artifact version/sha256). The
   pinned tuples remain the source of truth — a changed artifact can only fail loud, never
   alter behavior. P2 exit criterion "mapping API released and pinned" is met.
2. **PARKED: Multi-FE failover** (open P0-exit obligation, ledger §F NOT-DONE): only the comma-list
   URL *syntax* is proven, on a single-FE cluster. Needs a ≥3-FE cluster to prove
   follower/observer routing + failover before relying on it.

## PLAN deltas discovered by live evidence (consolidated)

Ledger §G items 1–11 stand (metadata truth = information_schema.columns.COLUMN_TYPE;
Decimal256 is a Trino-ceiling not wire limit; LARGEINT is text + getBigDecimal throws on the
negative extreme; getDate/getTimestamp YEAR/zone-shift hazards ⇒ LocalDate/LocalDateTime
reads; DOUBLE Infinity is a READ failure ⇒ getString reads; string-array decode provably
ambiguous ⇒ hard NO-GO; drop the per-scan RSMD probe; DATETIME max precision 6;
brikk FunctionHazard lacks target name + no release; multi-FE unproven; boolean
discrimination via tinyint(1)). New since the ledger:

- **The PLAN §6.2 `array_contains(...) = 1` wrapper defeats the inverted index**: profile
  counters show 0 rows index-filtered wrapped vs 997,997 bare
  (`evidence/inverted-index-explain-p2b.md`). Rendering is the BARE truthy form,
  predicate-equivalent and emitted only as top-level WHERE conjuncts (no composition rules
  for predicate-level rewrites — enforced structurally).
  **USER-APPROVED 2026-07-19** ("yes we want whatever is index eligible") — the bare truthy
  rendering is now the blessed contract, superseding the plan's `= 1` wrapper text.
- **Doris `arrays_overlap` matches NULL elements to each other** (`[1,null]×[null,4]` → 1
  where Trino → NULL): unguarded pushdown would over-return; fixed with the
  `array_filter(x -> x IS NOT NULL, left)` wrapper, all shapes re-proven.
- **Array string elements carry NO escaping** (spike correction of the P0 probe report's
  `repr()` artifact): the byte-identical ambiguity pair is the reason ARRAY<VARCHAR> is
  never native.
- **DATETIME array wire fractions are variable-width (1..6 = declared scale)** — the spike's
  fixed `.SSSSSS` parse was wrong for datetime(3); production parses 1–6 digits strictly.
- **P1b query_timeout finding**: Connector/J's client-side `Statement.setQueryTimeout` never
  fires during the streaming drain; server-side `SET query_timeout` kills send-blocked
  queries ("killed by timeout checker") — the session property applies server-side.
- **P3 TopN findings**: Doris 4.1.3 supports native `NULLS FIRST/LAST` (SR K6's ISNULL trick
  unnecessary); `default_order_by_limit = -1` (no bare-ORDER-BY truncation here — and the
  connector never emits bare ORDER BY); `LIMIT 70000 > 65535` exact ⇒ PLAN §9.2 risk closed
  for pushed shapes; Doris NOT/AND/OR three-valued logic is cell-identical to Trino's ⇒
  composition over value-identical operands (array_position comparisons) is safe, encoded as
  a structurally separate value-safe rewriter tier.
- Assembly note (P1a): `mysql-connector-j` is compile-scope (Driver referenced in code),
  matching upstream trino-mysql, vs the assembly-proof's `runtimeOnly` sketch.
- **P4 string probe findings (G5 resolved)**: Doris 4.1.3 string `=`/range/IN/LIKE/ORDER BY
  are pure byte (memcmp) semantics (`@@collation_connection = utf8mb4_0900_bin`) — identical
  to Trino VARCHAR, so BINARY needs no forcing mechanism (shares FULL rendering) and VARCHAR
  TopN is byte-order == codepoint-order safe. CHAR is the undetectable hazard (Doris stores
  unpadded + compares bytes; Trino trims — trailing-space data under-returns): CHAR excluded
  from GUARDED value pushdown and from TopN keys in ALL modes. LIKE: Doris default escape is
  `\` where Trino's no-escape LIKE is literal — exact fix is backslash-doubling; explicit
  ESCAPE passes through; wildcard-free LIKE never reaches the rule (engine folds to equality
  domain). A NUL-bearing-literal miss observed ONCE in the probe proved unreproducible
  (memory-pressure transient; reproductions byte-exact) — GUARDED keeps the 0x00 skip as
  defense-in-depth. Default = GUARDED (result-identical to NULL_ONLY by construction).
- **Ops note**: host memory pressure made the BE fail all queries with `MEM_ALLOC_FAILED`
  (host-available fell under the default 1.6% low-water mark);
  `max_sys_mem_available_low_water_mark_bytes = 268435456` set in compose/be.conf. Test JVM
  heap trimmed to 1536m, gradle daemon to 2g.

## Recommended next steps

1. **P3 remainder**: scalar predicate families one-at-a-time with live truth tables
   (column-to-column comparisons, arithmetic — the only shapes domains don't already cover);
   projection pushdown only where return type/shape is exact; guarded conditional rewrites
   (e.g. proven `char_length`); `arrays_overlap(col, ARRAY[...literal])` rendering;
   IPADDRESS parameter rendering (unlocks IP contains + IP TopN).
2. **P4 remainder — aggregates**: COUNT/MIN/MAX-non-text/exact SUM first, one family per proof
   (overflow/NULL/decimal-scale batteries), then remote statistics (Doris-specific
   `SHOW COLUMN STATS` — SR K12's information_schema queries do not transfer).
3. **Multi-FE failover cluster** (the standing P0-exit obligation) before any production
   claim about the comma-list URL.
4. Housekeeping candidates: bulk information_schema column listing (perf), MAP/STRUCT wire
   work, string-array superset pre-filter design (needs a retained-predicate mechanism).
